# Cloudflare Browser CDP

> A Cloudflare Worker that proxies Cloudflare Browser Rendering as a standard remote Chrome DevTools Protocol (CDP) endpoint over WebSocket. Connect with any CDP client using a shared secret to control a remote headless browser.

Key facts:

- Transparent CDP proxy — does not interpret or implement individual CDP methods
- Requires Cloudflare Workers Paid plan ($5/month) for Browser Rendering
- Authentication via `?secret=<CDP_SECRET>` query parameter on all endpoints
- WebSocket connection at `wss://<worker-url>/?secret=<CDP_SECRET>`
- All CDP methods supported by Cloudflare Browser Rendering are available
- Send CDP commands as JSON: `{"id": 1, "method": "Domain.method", "params": {...}}`
- Responses are JSON: `{"id": 1, "result": {...}}` or `{"id": 1, "error": {"code": -32000, "message": "..."}}`
- Events are JSON without id: `{"method": "Event.name", "params": {...}}`
- Browser session keep-alive is 10 minutes

## Connecting

WebSocket connection example:

```javascript
const ws = new WebSocket('wss://your-worker.workers.dev/?secret=YOUR_SECRET');

ws.onopen = () => {
  ws.send(JSON.stringify({ id: 1, method: 'Page.navigate', params: { url: 'https://example.com' } }));
};

ws.onmessage = (event) => {
  const response = JSON.parse(event.data);
  // Initial connection sends Target.targetCreated event with targetId
};
```

## Discovery Endpoints

The worker exposes Browserless-compatible discovery endpoints:

- `GET /json/version?secret=...` — returns browser version info and `webSocketDebuggerUrl`
- `GET /json/list?secret=...` — returns empty target list (placeholder)
- `GET /json?secret=...` — alias for `/json/list`
- `GET /` — info endpoint (no secret required, no sensitive data exposed)

## Common Patterns

Take a screenshot:
```javascript
await send('Page.navigate', { url: 'https://example.com' });
await new Promise(r => setTimeout(r, 3000)); // wait for load
const { data } = await send('Page.captureScreenshot', { format: 'png' });
const buffer = Buffer.from(data, 'base64');
```

Evaluate JavaScript:
```javascript
const { result } = await send('Runtime.evaluate', { expression: 'document.title' });
console.log(result.value); // page title
```

Set viewport for mobile:
```javascript
await send('Emulation.setDeviceMetricsOverride', { width: 375, height: 812, deviceScaleFactor: 2, mobile: true });
```

## OpenClaw Example

```json
{
  "browser": {
    "enabled": true,
    "defaultProfile": "cloudflare",
    "remoteCdpTimeoutMs": 10000,
    "remoteCdpHandshakeTimeoutMs": 20000,
    "profiles": {
      "cloudflare": {
        "cdpUrl": "wss://your-worker.workers.dev/?secret=<CDP_SECRET>"
      }
    }
  }
}
```

## Optional

- [Chrome DevTools Protocol Documentation](https://chromedevtools.github.io/devtools-protocol/): Full CDP reference
- [Cloudflare Browser Rendering](https://developers.cloudflare.com/browser-rendering/): Cloudflare's Browser Rendering docs
- [Puppeteer Documentation](https://pptr.dev/): The underlying browser automation library
